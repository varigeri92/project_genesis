
hi i have a Class for Calculating delta time:
	class Application;
	class Time
	{
		friend class  gns::Application;
	public:
		static float GetDelta() { return m_deltaTime; }
	private:
		static float m_deltaTime;
		static std::chrono::time_point<std::chrono::steady_clock> m_startTime;
		static void StartFrameTime()
		{
			
			m_startTime = std::chrono::high_resolution_clock::now();
			
		}
		static void EndFrameTime()
		{
			auto currentTime = std::chrono::high_resolution_clock::now();
			m_deltaTime = std::chrono::duration<float, std::chrono::seconds::period>(currentTime - m_startTime).count();
		}
	};

    Calling this In the Application.cpp like this:

    	while(!m_close)
        {
            Time::StartFrameTime();
            //other stuff!
            Time::EndFrameTime();
        }
I get this error:
1>Application.obj : error LNK2001: unresolved external symbol "private: static float gns::Time::m_deltaTime" (?m_deltaTime@Time@gns@@0MA)
1>Application.obj : error LNK2001: unresolved external symbol "private: static class std::chrono::time_point<struct std::chrono::steady_clock,class std::chrono::duration<__int64,struct std::ratio<1,1000000000> > > gns::Time::m_startTime" (?m_startTime@Time@gns@@0V?$time_point@Usteady_clock@chrono@std@@V?$duration@_JU?$ratio@$00$0DLJKMKAA@@std@@@23@@chrono@std@@A)
1>D:\GenesisEngine\Bin\Debug\Engine.dll : fatal error LNK1120: 2 unresolved externals